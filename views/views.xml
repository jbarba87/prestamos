<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="prestamos.list">
      <field name="name">prestamos list</field>
      <field name="model">prestamos.prestamos</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="prestamos.action_window">
      <field name="name">prestamos window</field>
      <field name="res_model">prestamos.prestamos</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="prestamos.action_server">
      <field name="name">prestamos server</field>
      <field name="model_id" ref="model_prestamos_prestamos"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="prestamos" id="prestamos.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="prestamos.menu_1" parent="prestamos.menu_root"/>
    <menuitem name="Menu 2" id="prestamos.menu_2" parent="prestamos.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="prestamos.menu_1_list" parent="prestamos.menu_1"
              action="prestamos.action_window"/>
    <menuitem name="Server to list" id="prestamos" parent="prestamos.menu_2"
              action="prestamos.action_server"/>
    -->
  </data>
</odoo>